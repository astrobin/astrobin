version: 0.2

phases:
  install:
    runtime-versions:
      docker: 19
      nodejs: 12

  build:
    commands:
      - buildspecs/build.sh

  post_build:
    commands:
      - buildspecs/push-image.sh
